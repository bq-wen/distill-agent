[tool.mypy]
python_version = "3.12"
exclude = ["\\.venv", "vendor", "frontend", "data", "dist", "build", "tests"]
ignore_missing_imports = true
warn_unused_ignores = false

[tool.pytest.ini_options]
testpaths = ["tests"]
